---
const steps = [
  {
    number: "01",
    title: "Initial Consultation",
    description: "Free strategy session to understand your innovation and business goals"
  },
  {
    number: "02",
    title: "Search & Analysis",
    description: "Comprehensive prior art search and patentability assessment"
  },
  {
    number: "03",
    title: "Application Drafting",
    description: "Strategic drafting to maximize protection and value"
  },
  {
    number: "04",
    title: "Filing & Prosecution",
    description: "Expert handling of office actions and examiner interviews"
  }
];
---

<section id="process" class="py-24 md:py-32 bg-stone-950 relative overflow-hidden">
  <!-- Background texture -->
  <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_center,rgba(245,158,11,0.15),transparent_70%)]"></div>
  
  <div class="relative z-10 max-w-7xl mx-auto px-6">
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-bold text-white mb-4 animate-on-scroll">
        Simple, Transparent Process
      </h2>
      <p class="text-xl text-stone-300 max-w-3xl mx-auto animate-on-scroll">
        From idea to protection in four straightforward steps
      </p>
    </div>
    
    <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">
      {steps.map((step, index) => (
        <div class="relative animate-on-scroll">
          {index < steps.length - 1 && (
            <div class="hidden lg:block absolute top-12 left-full w-full h-0.5 bg-gradient-to-r from-amber-500/50 to-transparent"></div>
          )}
          <div class="backdrop-blur-xl bg-white/5 border border-white/10 rounded-2xl p-8 hover:bg-white/10 hover:border-white/20 transition-all duration-300">
            <div class="text-5xl font-black gradient-text mb-4">{step.number}</div>
            <h3 class="text-xl font-bold text-white mb-3">{step.title}</h3>
            <p class="text-stone-400">{step.description}</p>
          </div>
        </div>
      ))}
    </div>
    
    <div class="text-center mt-12">
      <a href="https://strategymeeting.com" class="inline-flex items-center gap-3 px-8 py-4 backdrop-blur-xl bg-white/10 border border-white/20 rounded-full text-white font-semibold hover:bg-white/20 transition-all duration-300 animate-on-scroll">
        Start Your Journey
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
        </svg>
      </a>
    </div>
  </div>
</section>